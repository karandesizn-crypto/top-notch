# Security + Privacy Baseline

- Store provider secrets in Keychain.
- Do not log secrets or raw provider responses.
- Do not upload browser cookies/session cookies.
- Ask for the minimum macOS permissions required.
- Explain what data is collected before enabling sync/analytics.
- Local-only mode should work without an account.
- Backend should receive normalized app state, not provider credentials.
